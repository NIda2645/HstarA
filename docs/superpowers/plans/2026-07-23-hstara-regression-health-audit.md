# HstarA Regression Health Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore one coherent HstarA runtime across the shell, canvases, OpenShop, and API bridge; repair only reproduced regressions; remove only proven regenerable caches; and verify the main user workflows on port 3000.

**Architecture:** Treat `main.py`'s `OPENSHOP_RUNTIME_REVISION` as the backend cache-version contract and keep every OpenShop entry URL aligned with the editor runtime revision. Use existing contract, encoding, provider, persistence, and browser tests as evidence gates. Preserve API configuration, canvases, projects, assets, output, embedded runtimes, dependencies, and installer files.

**Tech Stack:** FastAPI, Python, browser JavaScript, OpenShop, Vitest, Node test runner, Playwright, PowerShell

---

### Task 1: Unify the OpenShop runtime revision

**Files:**
- Modify: `main.py`
- Modify: `static/index.html`
- Modify: `static/canvas.html`
- Modify: `static/smart-canvas.html`
- Test: `integrations/openshop/tests/hstar-openshop-host.test.js`
- Test: `integrations/openshop/tests/hstar-export-service.test.js`
- Test: `tools/tests/static-cache-integrity.test.mjs`

- [x] **Step 1: Reproduce the stale runtime failure**

Run `npm.cmd test -- --maxWorkers=1` in `integrations/openshop`.

Expected: the host/export revision tests fail because the editor runtime is newer than the formal shell entry points.

- [x] **Step 2: Align the backend and all formal entry points**

Align the OpenShop entry revision in the four production files listed above. The final audited revision is `2026.07.23.0003312127618`.

- [x] **Step 3: Verify the revision contracts**

Run the two targeted Vitest files and `node tools/tests/static-cache-integrity.test.mjs`.

Expected: all revision assertions pass and no `0003312127616` OpenShop entry reference remains.

### Task 2: Run the complete local correctness and encoding audit

**Files:**
- Test: `tools/tests/*.test.mjs`
- Test: `integrations/openshop/tests/**/*.test.js`
- Test: `tests/test_*.py`
- Test: `tools/audit-text-encoding.mjs`

- [x] **Step 1: Run every root Node contract test individually**

Enumerate `tools/tests/*.test.mjs`, execute each with Node, and stop with the exact filename on failure.

- [x] **Step 2: Run the complete OpenShop Vitest suite**

Run `npm.cmd test -- --maxWorkers=1` in `integrations/openshop`.

- [x] **Step 3: Run Python tests with an available test runtime**

Use an installed Python containing `pytest`, or install `pytest` only into `.codex-runtime/test-deps` and run the repository tests with that isolated dependency path.

- [x] **Step 4: Run encoding, JSON, JavaScript, and Python syntax checks**

Run the repository encoding audit, parse tracked JSON as UTF-8, run `node --check` over tracked JavaScript/MJS files, and parse tracked Python files with `ast.parse` without emitting bytecode.

Expected: zero source-encoding, parse, or syntax failures. PowerShell display-decoding artifacts do not count as source defects.

### Task 3: Verify canvas, API, persistence, and OpenShop workflows

**Files:**
- Test: `tools/tests/canvas-*.test.mjs`
- Test: `tools/tests/smart-*.test.mjs`
- Test: `tools/tests/*provider-routing.test.mjs`
- Test: `tools/tests/openshop-*.test.mjs`
- Test: `integrations/openshop/tests/*e2e.spec.js`

- [x] **Step 1: Verify shell and API health on port 3000**

Request the root shell, canvas pages, API settings, OpenShop runtime, and health endpoints. Confirm HTTP success and current revision URLs.

- [x] **Step 2: Browser-smoke the main shell and OpenShop tools**

Open the formal shell, enter a canvas OpenShop session, activate generative fill and local redraw, create a selection, and confirm no visible red error or uncaught runtime exception.

- [x] **Step 3: Verify project persistence without altering user projects**

Use the existing isolated project-storage tests; do not create or delete records in `data/projects.json` or user canvas storage manually.

### Task 4: Remove only approved regenerable residue

**Files:**
- Delete: `__pycache__/`
- Delete: `integrations/openshop/debug.log`
- Delete: `integrations/openshop/test-results/`

- [x] **Step 1: Resolve and validate each cleanup target**

Confirm every target is ignored, exists under the active worktree, and can be recreated. Do not delete `API/`, `python/`, `node_modules/`, `output/`, `tmp/`, assets, projects, history, installer data, or API configuration.

- [x] **Step 2: Remove the validated allowlist and re-run affected checks**

Expected: only generated bytecode/log/test-result residue is removed, with no new tracked Git changes.

### Task 5: Final verification

**Files:**
- Verify: all files changed by this audit

- [x] **Step 1: Rebuild the packaged OpenShop runtime**

Run `npm.cmd run build:hstar` and verify the generated `static/openshop` tree matches the approved runtime manifest.

- [x] **Step 2: Re-run failed and full suites**

Repeat the complete OpenShop suite, root Node suite, Python suite, encoding audit, static cache integrity test, and browser smoke path.

- [x] **Step 3: Review final diff and protected paths**

Run `git diff --check`, inspect the exact diff, confirm port 3000 remains available, and confirm protected user/configuration paths were not deleted or rewritten.
